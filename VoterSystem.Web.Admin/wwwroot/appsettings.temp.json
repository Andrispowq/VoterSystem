{
  "AppConfig": {
    "toastDurationInMillis": 3000,
    "hubBaseUrl": "${API_HTTP}",
    "enableTestUsers": "${ENABLE_TEST_USERS}"
  },
  "ApiBaseUrl": "${API_HTTP}",
  "AdminBaseUrl": "${ADMIN_HTTP}",
  "WebBaseUrl": "${WEB_HTTP}"
}
